.maine
  .item
    .itme__icons
      - unless @item == @items.first
        = link_to title_item_path(@title, @item) do
          = icon('fas', 'angle-left', class: "item__icons--left")
    .item__content
      = @item.item
    .itme__icons
      - unless @item == @items.last
        = link_to title_items_path do
          = icon('fas', 'angle-right', class: "item__icons--right")
        
  .content
    解説
    - @contents.each do |content|
      .content__comment
        .content__comment__text
          = content.content
        .content__comment__icon
          -# - if user_signed_in?
          -#   - if current_user.already_good?(content)
          -#     = link_to title_item_content_good_path(@title, @item, content, content), method: :delete do
          -#       = icon('fas', 'heart', class: "content__comment__icon--hart-red")
          -#     .content__comment__icon__count
          -#       = @goods.where(content_id: content).count
          -#   - else
          -#     = link_to title_item_content_goods_path(@title, @item, content), method: :post do
          -#       = icon('fas', 'heart', class: "content__comment__icon--hart")
          -#     .content__comment__icon__count
          -#       = @goods.where(content_id: content).count
          -# - else
          -#   = icon('fas', 'heart', class: "content__comment__icon--hart")
          -#   .content__comment__icon__count
          -#     = @goods.where(content_id: content).count
          = render partial: 'goods/good', locals: { content: content, title: @title, item_id: @item}
          .content__comment__icon__count
            = @goods.where(content_id: content).count
  .footer
    - if user_signed_in?
      .footer__plus
        = link_to new_title_item_path do
          = icon('fas', 'plus', class: "footer__plus--icon")
      .footer__textbox
        = form_for [@title, @item, @content], class: "footer__form" do |f|
          = f.text_field :content, class: "footer--text", cols: 10, rows: 3
          = f.submit "享受", class: "footer--submit"